export enum AncillaryProductEnum {
    Excursion, Transfer, Insurance, CarRental
}

export enum TransferTypeEnum {
    Shared, Private
}

export enum InsuranceTypeEnum {
    InsuranceType1, InsuranceType2
}

export enum StatusEnum {
    Pending, Approved, Denied
}

export enum TypeOfPassEnum {
    YCP, BCP, LMCP, NONE, IP
}

export enum NoteSectionEnum {
    Passengers, Flights, Hotels, Ancillaries, Booking, Reject
}

export enum NoteTypeEnum {
    Employee, Admin, Payload
}

export enum ActivityLogUpdateTypeEnum {
    Status, Data, Price
}

export enum ActivityLogSectionTypeEnum {
    Passengers, Flights, Hotels, Ancillaries, Booking, Reject
}

export enum ActivityLogUserTypeEnum {
    Employee, Admin, Payload
}