import { CardNumberControl } from './card-number-control';

describe('CardNumberControl', () => {
  it('should create an instance', () => {
    expect(new CardNumberControl()).toBeTruthy();
  });
});
